# chrome-extension-boilerplate

> Chrome extension with Typescript boillerplate for starter

## Install

```(install)
npm install
```

## Usage

```(usage)
npm run watch
```

## Build

```(build)
npm run build
```
